import styled from 'styled-components';

const Container = styled.div`
  width: 270px;
  height: 540px;
  border-radius: 6px;
  background: var(--color-orange);
`;

const CardImage = styled.img``;

const SaibaMais = styled.button``;

export { Container, CardImage, SaibaMais };
